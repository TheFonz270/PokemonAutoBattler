import React, {useState, useEffect} from 'react';

const RandomiseMove = ({trainer, pokemon}) => {

    return (
        <div class="randomiseMove">
            <button>RandomiseMove</button>
        </div>
    )
}

export default RandomiseMove;